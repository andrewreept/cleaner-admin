/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you ever re-enable experimental features, put them here.
  // experimental: {},
};

module.exports = nextConfig;